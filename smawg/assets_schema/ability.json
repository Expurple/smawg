{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Ability",
    "description": "In-game ability (like on a physical ability banner).",
    "type": "object",
    "properties": {
        "name": {
            "description": "The displayed name of the ability.",
            "type": "string"
        },
        "n_tokens": {
            "description": "The number of additional race tokens the player gets.",
            "type": "integer",
            "minimum": 0
        }
    },
    "required": ["name", "n_tokens"]
}
