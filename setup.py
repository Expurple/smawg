#!/usr/bin/env python3

"""
Legacy installation method.

The recommended way to install is using `pip`, e.g. `pip install --user .`

For package metadata, see `setup.cfg`.
"""

from setuptools import setup

setup()
